import 'package:flutter/material.dart';

class ActiveGracePeriod extends StatelessWidget {
  static const routeName = '/active-grace-period';
  const ActiveGracePeriod({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
