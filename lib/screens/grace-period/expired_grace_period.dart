import 'package:flutter/material.dart';

class ExpiredGracePeriod extends StatelessWidget {
  static const routeName = '/expired-grace-period';
  const ExpiredGracePeriod({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
