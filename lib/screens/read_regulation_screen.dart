import 'package:flutter/material.dart';

class ReadRegulationScreen extends StatelessWidget {
  static const routeName = '/read-regulation';
  const ReadRegulationScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
